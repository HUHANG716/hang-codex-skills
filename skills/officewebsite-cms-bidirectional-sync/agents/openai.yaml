interface:
  display_name: "Officewebsite CMS Sync"
  short_description: "Evaluate, generate, and synchronize officewebsite CMS contracts"
  default_prompt: "Use $officewebsite-cms-bidirectional-sync for officewebsite CMS field review, schema work, component wiring, or bidirectional sync."
